using static Helper;

public class AIGM_Transform
{
    public Vector3 Position;
    public Vector3 Direction;
    public float Size;

    public AIGM_Transform(Vector3 position, Vector3 direction, float size)
    {
        Position = position;
        Direction = direction;
        Size = size;
    }
}
